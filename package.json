{
  "name": "pryzael-mcp-worker",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:mcp-catalog": "node scripts/generate_mcp_catalog.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node scripts/generate_mcp_catalog.mjs && node --test worker/index.test.mjs && wrangler deploy --dry-run"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "wrangler": "4.127.0"
  }
}
